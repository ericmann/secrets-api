/vendor/
/node_modules/
/coverage/
/.phpunit.result.cache
/.phpcs.cache
/phpcs.xml
/phpunit.xml
/phpunit-multisite.xml
/phpstan.neon
.DS_Store

# Platform bindings vendor their own SDKs; never the plugin.
examples/*/vendor/

# wp-env local overrides — may contain real credentials.
.wp-env.override.json

# Docs site build artefacts (site/ is an Astro project).
site/node_modules/
site/dist/
site/.astro/

# Spacefast CLI link and state. Written wherever sf publish runs from; never commit it.
.spacefast/
.foundry/implement.lock

# The WP-CLI smoke test's throwaway install; see bin/smoke-install.sh.
/.smoke/
